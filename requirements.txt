# Runtime dependencies.
#
# Lower bounds only for now; Milestone 3 pins these exactly for reproducible
# Docker builds. Install with: pip install -r requirements.txt

# --- Configuration --------------------------------------------------------
pydantic>=2.5
pydantic-settings>=2.0
python-dotenv>=1.0

# --- PDF parsing ----------------------------------------------------------
pymupdf>=1.23
pdfplumber>=0.10
PyPDF2>=3.0

# --- Chunking -------------------------------------------------------------
# The full `langchain` + `langchain-community` packages were previously pulled
# in for a single text splitter; this is that same class without the framework.
langchain-text-splitters>=0.2
tiktoken>=0.5

# --- Embeddings and reranking ---------------------------------------------
sentence-transformers>=2.3
# torch arrives via sentence-transformers; pin it explicitly only if you need
# a specific CUDA build.

# --- Vector store ---------------------------------------------------------
chromadb>=0.5

# --- Lexical search -------------------------------------------------------
rank-bm25>=0.2.2
numpy>=1.24

# --- LLM providers (only the one you use is needed at runtime) ------------
groq>=0.4.1
openai>=1.40
google-generativeai>=0.8

# --- HTTP API -------------------------------------------------------------
# fastapi and uvicorn already arrive transitively via chromadb, but the API
# depends on them directly and an undeclared dependency breaks the moment the
# transitive one is dropped.
fastapi>=0.110
uvicorn[standard]>=0.27
# Required by FastAPI to parse multipart uploads. Without it, POST /papers
# fails at request time with a RuntimeError rather than at import.
python-multipart>=0.0.9

# --- UI -------------------------------------------------------------------
streamlit>=1.29
